{
  "schema_version": 2,
  "context_budget": {
    "agents_max_bytes": 4500,
    "default_profile_max_active_skills": 12,
    "default_profile_max_catalog_bytes": 4000,
    "skill_search_default_limit": 3,
    "skill_search_max_limit": 20,
    "skill_search_max_output_bytes": 2048
  },
  "profiles": [
    {
      "name": "minimal",
      "description": "极简运行配置，适合资产 smoke、基础操作和低上下文实验",
      "max_parallel_agents": 2,
      "max_depth": 2,
      "default_agent_type": "default",
      "enabled_mcp_groups": [
        "core"
      ],
      "notes": "极简 profile；当前仅激活 caveman，不注入受管 Custom Agent 或 Workflow"
    },
    {
      "name": "solo-dev",
      "description": "个人深度开发配置",
      "max_parallel_agents": 4,
      "max_depth": 3,
      "default_agent_type": "worker",
      "enabled_mcp_groups": [
        "core",
        "dev"
      ],
      "notes": "个人专项 Skill、5 个本地 Agent，以及归档、资产治理和会话接力 Workflow"
    },
    {
      "name": "team-collab",
      "description": "团队协作配置",
      "max_parallel_agents": 6,
      "max_depth": 4,
      "default_agent_type": "worker",
      "enabled_mcp_groups": [
        "core",
        "dev",
        "design"
      ],
      "notes": "adk-first 协作配置；外部参考不进入运行时 profile 或 fallback"
    },
    {
      "name": "token-lean",
      "description": "默认低上下文配置，常驻核心路由与交付门禁，长尾 skill 按查询延迟加载",
      "catalog_mode": "lazy",
      "max_parallel_agents": 4,
      "max_depth": 3,
      "default_agent_type": "default",
      "enabled_mcp_groups": [
        "core"
      ],
      "notes": "默认 profile；最多暴露 12 个通用 skill，协作、Git 收尾、资产治理和嵌入式能力按需延迟加载"
    }
  ]
}
