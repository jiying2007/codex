{
  "schema_version": 2,
  "context_budget": {
    "agents_max_bytes": 10000,
    "default_profile_max_active_skills": 20,
    "default_profile_max_catalog_bytes": 6000,
    "skill_search_default_limit": 5,
    "skill_search_max_limit": 20,
    "skill_search_max_output_bytes": 4096
  },
  "profiles": [
    {
      "name": "minimal",
      "description": "极简运行配置，适合资产 smoke、基础操作和低上下文实验",
      "max_parallel_agents": 2,
      "max_depth": 2,
      "default_agent_type": "default",
      "enabled_mcp_groups": [
        "core"
      ],
      "notes": "极简 profile；当前仅激活 caveman，不注入受管 Custom Agent 或 Workflow"
    },
    {
      "name": "solo-dev",
      "description": "个人深度开发配置",
      "max_parallel_agents": 4,
      "max_depth": 3,
      "default_agent_type": "worker",
      "enabled_mcp_groups": [
        "core",
        "dev"
      ],
      "notes": "个人专项 Skill、5 个本地 Agent，以及归档、资产治理和会话接力 Workflow"
    },
    {
      "name": "team-collab",
      "description": "团队协作配置",
      "max_parallel_agents": 6,
      "max_depth": 4,
      "default_agent_type": "worker",
      "enabled_mcp_groups": [
        "core",
        "dev",
        "design"
      ],
      "notes": "adk-first 协作配置；Superpowers 已移入 superpowers-compat fallback profile"
    },
    {
      "name": "token-lean",
      "description": "默认低上下文配置，常驻核心路由与交付门禁，长尾 skill 按查询延迟加载",
      "catalog_mode": "lazy",
      "max_parallel_agents": 4,
      "max_depth": 3,
      "default_agent_type": "default",
      "enabled_mcp_groups": [
        "core"
      ],
      "notes": "默认 profile；最多暴露 20 个 skill，完整兼容集合保留在 team-collab"
    },
    {
      "name": "superpowers-compat",
      "description": "显式 Superpowers 兼容配置，仅用于用户点名、回归对照或 ADK 不适用的 fallback",
      "max_parallel_agents": 4,
      "max_depth": 3,
      "default_agent_type": "worker",
      "enabled_mcp_groups": [
        "core",
        "dev"
      ],
      "notes": "不作为默认 profile；仅激活 Superpowers 兼容 Skill，不与默认 token-lean 的 ADK 主干混用"
    }
  ]
}
