# Control Reference

这些文档来自 v1 `codex-home/control`，现在只作为仓库内参考资料维护，不再注入 `~/.codex/control`。

v2 运行态 `control` 只保留：

- `control/agents-local/`
- `control/state/`

实际构建、注入、检查和回滚入口统一位于仓库根目录的 `scripts/`。
