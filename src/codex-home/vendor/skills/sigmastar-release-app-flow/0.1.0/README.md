# sigmastar-release-app-flow

Local Chronicle-derived companion skill for PCR02/SigmaStar `build.sh` app/image/OTA/customer partition and lightweight build-order issues. Generic release orchestration stays with `adk-embedded-release-orchestration`.
